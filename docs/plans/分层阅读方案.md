
# DeepReader 阅读层次架构重构计划

## Context

DeepReader 是一款智能阅读插件，需要将《如何阅读一本书》的**四个阅读层次**融入 Agent 工作逻辑，让 Agent 具备像真人一样的阅读思维模式。

### 四个阅读层次

| 层次 | 名称 | 特点 | 工具映射 |
|------|------|------|----------|
| 1 | 基础阅读 | 认字、理解句子 | search_doc（精准查询） |
| 2 | 检视阅读 | 有限时间内抓重点 | get_toc, search_doc |
| 3 | 分析阅读 | 无限时间完整阅读 | get_chapter（多次调用） |
| 4 | 主题阅读 | 跨书比较分析 | search_read_books |

### 设计原则

- **层次递进**：高层次包含低层次能力
- **自动判断**：Agent 根据问题特征自动选择层次
- **状态可见**：用户能看到当前使用的阅读层次

---

## Implementation Plan

### Phase 1：核心类型定义

**文件**：`frontend/src/agent/ui/humanized-types.ts`

1. 添加 `ReadingLevel` 类型：
```typescript
export type ReadingLevel = 'elementary' | 'inspectional' | 'analytical' | 'syntopical';
```

2. 添加工具到阅读层次的映射：
```typescript
export const TOOL_TO_READING_LEVEL: Record<string, ReadingLevel> = {
  get_toc: 'inspectional',
  search_doc: 'inspectional',
  get_chapter: 'analytical',
  search_read_books: 'syntopical',
};
```

3. 添加阅读层次描述：
```typescript
export const READING_LEVEL_DESCRIPTIONS: Record<ReadingLevel, { name: string; action: string; icon: string }> = {
  elementary: { name: '基础阅读', action: '理解基本概念', icon: '📖' },
  inspectional: { name: '检视阅读', action: '浏览结构，抓取要点', icon: '🔍' },
  analytical: { name: '分析阅读', action: '深入阅读，咀嚼消化', icon: '🧐' },
  syntopical: { name: '主题阅读', action: '跨书比较，建立关联', icon: '📚' },
};
```

4. 扩展 `HumanizedProgress` 接口：
```typescript
export interface HumanizedProgress {
  // ... 现有字段
  currentReadingLevel?: ReadingLevel;
  readingLevelDescription?: string;
}
```

---

### Phase 2：系统提示增强

**文件**：`frontend/src/agent/context/builder.ts`

1. 新增 `buildReadingLevelLayer()` 方法，定义阅读层次方法论

2. 在 `buildConstraints()` 中添加阅读层次选择规则：
   - 检视阅读信号："这本书讲什么"、"总结一下"
   - 分析阅读信号："详细解释"、"为什么"、"深入分析"
   - 主题阅读信号："比较"、"其他书"、"关联"

3. Agent 应在思考时表明当前阅读层次

---

### Phase 3：适配器增强

**文件**：`frontend/src/agent/ui/humanized-adapter.ts`

1. 添加 `currentReadingLevel` 字段

2. 新增 `determineReadingLevel()` 方法：
   - 检查当前运行的工具
   - 根据工具类型确定阅读层次
   - 返回最高层次

3. 修改 `toHumanizedProgress()` 返回阅读层次信息

---

### Phase 4：UI 展示

**文件**：
- `frontend/src/agent/ui/humanized-view.ts`
- `frontend/src/styles/agent-humanized.css`

1. 修改状态元素创建函数，添加阅读层次徽章

2. 添加 CSS 样式：
```css
.reading-level-badge {
  font-size: 0.85em;
  padding: 2px 8px;
  border-radius: 12px;
  margin-bottom: 8px;
}
.reading-level-inspectional { background: #e3f2fd; color: #1565c0; }
.reading-level-analytical { background: #fff3e0; color: #e65100; }
.reading-level-syntopical { background: #f3e5f5; color: #7b1fa2; }
```

---

### Phase 5：工具描述优化（可选）

更新工具的 description，说明其对应的阅读层次：
- `get_toc` → "检视阅读：快速了解书籍结构"
- `get_chapter` → "分析阅读：深入阅读特定章节"
- `search_read_books` → "主题阅读：跨书籍搜索相关内容"

---

## Critical Files

| 文件 | 修改类型 |
|------|----------|
| `frontend/src/agent/ui/humanized-types.ts` | 新增类型和映射 |
| `frontend/src/agent/ui/humanized-adapter.ts` | 核心逻辑 |
| `frontend/src/agent/context/builder.ts` | 系统提示 |
| `frontend/src/agent/ui/humanized-view.ts` | UI 渲染 |
| `frontend/src/styles/agent-humanized.css` | 样式 |

---

## Verification

1. **功能测试**：
   - 发送"这本书讲什么"→ 应触发检视阅读
   - 发送"详细解释一下这个概念"→ 应触发分析阅读
   - 发送"比较一下两本书的观点"→ 应触发主题阅读

2. **UI 验证**：
   - 状态显示应包含阅读层次徽章
   - 层次切换时徽章应更新

3. **命令**：
```bash
# 构建部署
cd /Users/lizhao/workspace/DeepReader/frontend && npm run deploy && obsidian plugin:reload id=deepreader

# 调试测试
obsidian dev:console clear && obsidian command id=deepreader:debug-send-message && sleep 5 && obsidian dev:console
```
