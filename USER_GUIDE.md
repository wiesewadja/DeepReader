# DeepPDF 用户指南

DeepPDF 是一个 Obsidian 插件，通过 AI 驱动的 PDF 智能索引，让您能够与 PDF 文档进行自然语言对话。

## 工作流程

### 1. 上传与索引

1. **打开索引管理器**
   - 点击顶部导航栏的索引图标，或使用侧边栏
   - 在索引管理器中点击"新建索引"

2. **上传 PDF 文件**
   - 选择要索引的 PDF 文件
   - 等待上传完成

3. **等待索引完成**
   - 系统会自动解析 PDF 内容
   - 提取文本、分块并创建语义向量索引
   - 索引进度会在顶部状态栏显示

### 2. 生成 Markdown 笔记

索引完成后，您可以：

1. **选择已索引的 PDF**
   - 在索引管理器中点击 PDF 名称
   - 查看索引元信息（页数、块数等）

2. **生成结构化笔记**
   - 点击"生成笔记"按钮
   - 系统会基于 PDF 内容生成 Markdown 格式的笔记
   - 笔记包含：摘要、关键点、章节结构等

### 3. 智能问答

1. **打开聊天界面**
   - 在侧边栏中选择已索引的 PDF
   - 开始对话

2. **提问**
   - 用自然语言提问，例如：
     - "这份文档主要讲了什么？"
     - "第三章的核心观点是什么？"
     - "找出所有关于机器学习的内容"

3. **AI 回答**
   - 系统会基于 PDF 内容给出准确回答
   - 回答会标注来源页码
   - 点击引用可直接跳转到对应位置

### 4. 引用跳转

当 AI 回答包含引用时：

1. **查看引用来源**
   - 每个引用显示文件名和页码
   - 包含原文片段供验证

2. **一键跳转**
   - 点击"跳转"按钮
   - PDF 阅读器会自动打开并定位到对应页面

## 界面说明

### 顶部导航栏

- **Logo + 标题**: DeepPDF 品牌标识
- **状态指示器**:
  - 🟢 绿色: 服务连接正常
  - 🟡 黄色: 正在处理请求
  - 🔴 红色: 服务连接失败
- **设置按钮**: 打开插件设置

### 索引管理器

- **折叠面板**: 点击标题可展开/收起
- **索引列表**: 显示所有已索引的 PDF
- **当前激活**: 高亮显示当前选中的索引
- **新建索引**: 创建新的 PDF 索引

### 聊天界面

- **消息区域**:
  - 用户消息：右侧蓝色气泡
  - AI 回复：左侧灰色气泡
- **输入框**: ChatGPT 风格的底部输入框
- **引用卡片**: 显示回答来源的 PDF 片段

## 快捷操作

| 操作 | 说明 |
|------|------|
| `Cmd+R` | 重新加载插件 |
| 点击状态 | 查看连接详情 |
| 点击引用 | 跳转到 PDF 页面 |
| 点击索引标题 | 折叠/展开索引列表 |

## 故障排除

### 状态显示红色

1. 检查后端服务是否运行：
   ```bash
   cd backend
   uv run uvicorn deeppdf.main:app --port 6088 --reload --loop asyncio
   ```

2. 确认端口设置（默认 6088）

### 索引失败

1. 检查 PDF 文件是否损坏
2. 确认 PDF 包含可提取的文本（非扫描图片）
3. 查看后端日志获取详细错误信息

### 回答不准确

1. 确认 PDF 已完整索引
2. 尝试更具体的提问
3. 检查引用来源验证回答

## 技术支持

- **GitHub Issues**: https://github.com/your-repo/deeppdf/issues
- **文档**: 查看 `/docs` 端点（后端运行时访问 http://localhost:6088/docs）
